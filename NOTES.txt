06.03.2018, 20:30, KM
---------------------------------------------------------------------
Wrzucilem dokument projektowy oraz podlinkowana solucje w VS2015 do SFMLa w konfiguracji w dzialacej konfiguracji x64 oraz prawie dzialajacej konfiguracji x86. 
Jest problem z podlinkowaniem jednoczesnie wersji 64 oraz 32 bitowej, wynika to z bledu SFML, przez ktory w katalogu z projektem na zywca musza byc wrzucone dll'ki. Problem polega na tym, ze DLL 64 oraz 32 bitowe nazywaja sie tak samo i o ile konfiguracja x86 buduje sie poprawnie, to podpina DLL x64 (które aktualnie s¹ w katalogu z projektem w celu zapewnienia dzia³ania wersji 64 bitowej) co koñczy siê crashem. Spróbujê to naprawiæ. 

Druga sprawa: wa¿na jest tutaj lokalizacja, w której mamy zainstalowanego SFMLa. Nie chcê wrzucaæ SFMLa na gita, ¿eby niepotrzebnie nie zwiêkszaæ rozmiarów repozytorium, wyd³u¿y nam siê przez to praca z samym gitem. Za³o¿y³em, ¿e SFML znajduje siê w nastêpuj¹cych lokalizacjach:
Bêd¹c w katalogu nadrzêdnym do repozytorium tworzymy katalog Dependencies. W nim tworzymy katalog x86, do którego wrzucamy wersjê x86 SFMLa (je¿eli jej nie macie to nic siê nie stanie, nadal powinna dzia³aæ wersja 64 bitowa), natomiast do samego katalogu Dependecies wrzucamy wersje 64-bitow¹ SFMLa. Je¿eli ustawimy siê w katalogu, które tworzy nam siê po git clone, œcie¿ki wygl¹daj¹ tak:
(x64) - ..\Dependencies\SFML-2.4.2
(x86) - ..\Dependencies\x86\SFML-2.4.2

Ostatnia sprawa: Chcia³bym, ¿eby ten pliczek nam pos³u¿y³ jako taki swego rodzaju dzienniczek, w którym bêdziemy wpisywali problemy, jakie napotkamy w trakcie pracy nad czymœ lub jak mamy coœ do przekazania - czat na facebooku jest problematyczny, wiadomosci szybko przelatuj¹, tutaj bêdziemy wpisywaæ po prostu tylko wa¿ne rzeczy. 
---------------------------------------------------------------------

Metoda Map::GetTexture powinna byæ const.
Mo¿na siê zastanowiæ nad przeniesieniem TerrainType do Map.

----------------------------------------------------------------------
11.03.18 KM

Zintegrowane rysowanie mapy z logik¹, mapa wczytywana jest z pliku tekstowego w formacie:
X(szerokoœæ mapy) Y(wysokoœæ mapy)
<X*Y liczb opisuj¹cych mapê oddzielonych dowolnym znakiem bia³ym>
Zaimplementowano regu³y przejœcia po mapie w postaci 4 bitowej tablicy flag. Flagi odczytywane s¹ z mapy, a zwi¹zku z tym wzór
na liczbê opisuj¹c¹ punkt na mapie jest nastêpuj¹cy:
P = TerrainType (aktualnie 0, 1 lub 2, mo¿na rozszerzyæ do 31 w³¹cznie tekstur) + UpFlag * 32 + RightFlag * 64 + DownFlag * 128 + LeftFlag * 256
UpFlag, RightFlag, DownFlag oraz LeftFlag wynosz¹ 0 lub 1, 1 w przypadku mo¿liwoœci ruchu z punktu w zadanym kierunku
Przyk³adowy punkt umo¿liwiaj¹cy przejœcie w rozwidleniu w dó³ oraz w prawo opisany tekstur¹ o numerku 1 ma wartoœæ 1+64+128 = 193
Przyk³adowa mapa znajduje siê w pliku Map.txt

So far so good, ale wymaga dobrego przetestowania. 